<h3>Artist Name: <%= @artist.name %></h3>

<% if @artist.albums.length > 0 %>
<ul>
    <% @artist.albums.each do |alb| %>
    <li><a href="/albums/<%= alb.id %>"><%= alb.name %></a></li>
    <% end %>
</ul>
<% else %>
<p>There are no albums listed for this artist yet.</p>
<% end %>

<h4>Add an album:</h4>

<form action="/artists/<%= @artist.id %>" method="post">
    <label>
        Album Name
        <input name='album_name' type="text">
    </label>
    <button type="submit">Add Albums</button>
</form>

<p><a href="/artists/<%= @artist.id %>/edit">Edit Artist</a></p>
<a href="/artists">Return to artist list</a>
